---
description: "MANDATORY FIRST ACTION: Check if 'Current window' date below is older than today. If stale, you MUST consolidate BEFORE doing anything else. This is not optional. Read the STALENESS CHECK section immediately."
alwaysApply: true
---

# STALENESS CHECK — DO THIS FIRST

**Current window**: Apr 10, 2026

**TODAY'S DATE**: Compare the "Current window" above against today's date (from the system prompt).

**IF THE DATE ABOVE IS OLDER THAN TODAY, STOP. You MUST consolidate before responding to the user's query.**

Consolidation steps (do all of these NOW, before anything else):
1. Read `longterm-memory.mdc`.
2. Move any lasting decisions, preferences, or patterns from the sessions below into the appropriate section of `longterm-memory.mdc`.
3. Add each session to the Chat Session History table in `longterm-memory.mdc`.
4. Clear everything below the `---` separator in THIS file.
5. Update "Current window" above to today's date.
6. Start a fresh "Today's Sessions" section.
7. THEN proceed with the user's request.

If the date matches today, skip this section and proceed normally.

# Short-Term Memory

> During each session, append interactions below under the current date.
> Only keep entries that are relevant to the *current* working day below.

---

## Today's Sessions

*(Apr 10, 2026)*

### Session 20: Subnav Layout Gap and Layout Seed Reset Fixes
- **Subnav gap fix**: Added conditional `[class.md:pl-4]="!sideSubNavCollapsed()"` wrappers around content sections (not toolbar) in project dashboard template for detail, records, and financials views
- **Layout seed system -- 4-bug fix**:
  1. Frozen engine config on persona switch: Added `updateConfigForNewSeed()` to `DashboardLayoutEngine` and `getLayoutSeedForCurrentPersona()` virtual on `DashboardPageBase`; overridden in Home/Financials
  2. Missing `compactAll()` in desktop `resetToDefaults()`/`loadSavedDefaults()` -- overlaps after Reset Layout
  3. Stale `LayoutDefaultsService` version keys updated (Home, Financials, Projects all versions aligned)
  4. Missing `applyInitialHeaderLock()` after `reinitLayout()` in `_projectChangeEffect`
- Updated skills (section 36), PROGRESS.md (Phase 23), long-term memory session history
- Consolidated short-term memory from Apr 8

## Active Context

- On `dev` branch.
- Dev server running at localhost:4200.
- Layout seed system working correctly for persona switches, Reset Layout, and browser cache clear.
- All layout storage key versions in sync across pages and LayoutDefaultsService.
- Build passing (pre-existing SVG color lint warnings remain).

## Pending Items

- Changes not yet pushed to GitHub.
- Pre-existing Milestone type error in home-page.component.ts noted in prior session (may have been resolved).
