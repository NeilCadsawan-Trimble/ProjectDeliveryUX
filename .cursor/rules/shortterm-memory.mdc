---
description: "MANDATORY FIRST ACTION: Check if 'Current window' date below is older than today. If stale, you MUST consolidate BEFORE doing anything else. This is not optional. Read the STALENESS CHECK section immediately."
alwaysApply: true
---

# STALENESS CHECK — DO THIS FIRST

**Current window**: Apr 3, 2026

**TODAY'S DATE**: Compare the "Current window" above against today's date (from the system prompt).

**IF THE DATE ABOVE IS OLDER THAN TODAY, STOP. You MUST consolidate before responding to the user's query.**

Consolidation steps (do all of these NOW, before anything else):
1. Read `longterm-memory.mdc`.
2. Move any lasting decisions, preferences, or patterns from the sessions below into the appropriate section of `longterm-memory.mdc`.
3. Add each session to the Chat Session History table in `longterm-memory.mdc`.
4. Clear everything below the `---` separator in THIS file.
5. Update "Current window" above to today's date.
6. Start a fresh "Today's Sessions" section.
7. THEN proceed with the user's request.

If the date matches today, skip this section and proceed normally.

# Short-Term Memory

> During each session, append interactions below under the current date.
> Only keep entries that are relevant to the *current* working day below.

---

## Today's Sessions

### Session 11 (Apr 3)
- **Topic**: Memory consolidation
- **What happened**:
 1. Consolidated short-term memory (stale since Mar 31) into long-term memory.
 2. Moved sessions 9-10 to history table. Added canvas zoom decisions, planned features (Google Cloud / webmail / calendar), updated test counts (84 unit / 325 static), added file map entries, added canvas zoom macOS fragile area.

### Session 12 (Apr 3)
- **Topic**: Project risk distribution, sort ordering, "Sort by Priority" menu
- **What happened**:
 1. Fixed project risk distribution to match target: 3 None, 3 Low, 1 Moderate, 1 High.
   - Fixed `seen` key normalization in `buildUrgentNeeds` (leading zeros mismatch).
   - Fixed year-less submittal dates in `dashboard-data.seed.ts`.
   - Introduced `coreWarningCount` (excludes change-order warnings) for badge determination.
 2. Fixed tile visual ordering: created `TILE_VISUAL_ORDER` constant matching grid layout (proj1, proj2, proj3, proj6, proj7, proj4, proj5, proj8) since proj4-7 positions don't follow numeric order in the grid.
 3. Updated sort function to use `coreWarningCount` instead of full `warningCount`, aligning sort order with badge logic.
 4. Added `AgentProjectData.coreWarningCount` to the interface and updated all tests (54 pass).
 5. Added "Sort by Priority" menu item to both canvas and desktop Layout flyout menus. Only visible on `/projects` page. Resets tile positions to default priority order.

### Session 13 (Apr 3)
- **Topic**: RFI/Submittal compact mode on project dashboard
- **What happened**:
  1. Ported compact mode from home page to project dashboard for RFI and Submittal widgets.
  2. Added `isRfiCompact` / `isSubmittalCompact` computed signals (colSpan <= 5 or mobile).
  3. Added `rfiCompactItems` / `submittalCompactItems` data arrays and `rfiStatusCounts` / `submittalStatusCounts` computed signals.
  4. Updated HTML template with `@if (isRfiCompact())` branching -- compact shows status summary tiles, full shows table.
  5. Added `navigateToRfiList()` / `navigateToSubmittalList()` methods for compact tile click actions.
  6. Updated skills (`dashboard-layout-lessons` section 22) and PROGRESS.md (Phase 16 expanded to 7 items).

### Session 14 (Apr 3)
- **Topic**: Navbar native rendering fallback skill documentation
- **What happened**:
  1. Added section 23 to `dashboard-layout-lessons` SKILL.md: "Modus Navbar Native Rendering Fallback (Stencil/Angular 20 Incompatibility)".
  2. Documents the complete architecture: runtime detection via `navbarNativeRendered` signal, conditional fallback elements (Trimble logo, hamburger, search, notifications, help), correct icon ordering, background color consistency (`bg-card`), and the 4 navbar instances that must stay in sync.
  3. Updated skill description to include navbar modification trigger.
  4. Added entry to Known Fragile Areas table in longterm-memory.mdc.
  5. Updated skill description in longterm-memory.mdc.

### Session 15 (Apr 3)
- **Topic**: Hamburger / side nav not expanding (shell + project dashboard)
- **What happened**:
  1. Removed `_reattachHamburgerEffect` and `attachHamburgerListener` from `dashboard-shell.component.ts`; they set `_hamburgerAttached` and blocked `onMainMenuToggle` from calling `navExpanded.set(open)` when native toolbar rendered.
  2. `onMainMenuToggle` now always `this.navExpanded.set(open)`.
  3. Project dashboard: added `(mainMenuOpenChange)="onMainMenuToggle($event)"` on canvas and desktop `modus-navbar`; removed `attachHamburgerListener`, `hamburgerBtn`, and `hamburgerEffect` (native path had no output binding before).
  4. Updated `tests/static/dashboard-shell.spec.ts` and `dashboard-layout-lessons` section 16.

### Session 16 (Apr 3)
- **Topic**: Canvas hamburger vs sidenav horizontal alignment
- **What happened**:
  1. In `src/styles.css` (canvas `@media (min-width: 1920px)`), fixed hamburger used `left: 12px` and 32px hit target while `.custom-side-nav-item.selected` uses 4px margin + 48px tile on the 56px rail, so the menu sat ~8px to the right of the home tile.
  2. Updated native `modus-wc-button[aria-label='Main menu']` and `.shell-navbar-hamburger` to `left: 4px; top: 4px;` with 48x48 box and `border-radius: 0.375rem` on fallback to match selected rail tiles; kept `padding-left: 56px` on start slot.

## Active Context

- On `dev` branch.
- Hamburger / mainMenuOpenChange fix (session 15) + prior unpushed work; verify in browser after pull.
- Pre-existing SVG color lint violations remain (17 AI icon gradients) -- not related to changes.

## Pending Items

- Push changes when user requests.
