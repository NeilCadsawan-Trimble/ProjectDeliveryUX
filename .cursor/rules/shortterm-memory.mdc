---
description: Short-term memory for the current 24-hour session window. Records interactions, decisions, and context from today. At the start of a new day, consolidate meaningful entries into longterm-memory.mdc, then reset this file.
alwaysApply: true
---

# Short-Term Memory

**Current window**: Mar 29, 2026

> **Lifecycle**:
> 1. During each session, append interactions below under the current date.
> 2. When the date in "Current window" is older than today, **consolidate first**:
>    - Move any lasting decisions, preferences, or patterns into `longterm-memory.mdc` under the appropriate section.
>    - Add the session to the Chat Session History table in `longterm-memory.mdc`.
>    - Clear everything below the `---` separator in this file and update "Current window" to today's date.
> 3. Only keep entries that are relevant to the *current* working day below.

---

## Today's Sessions

### Session 7 (Mar 29)
- **Topic**: Collapsible subnav redesign
- **What happened**:
  1. Rewrote `CollapsibleSubnavComponent` collapse behavior: items list rolls up/down via `max-height` animation instead of width collapse.
  2. Chevron rotates: `chevron_left` at 0deg (points left, collapsed) and -90deg (points down, expanded), animated via `transition-transform duration-200`.
  3. Collapsed header shows active item label instead of section title.
  4. When collapsed, subnav layout width goes to 0 so title/widgets slide left (16px from edge). Subnav header floats visually at 227px via absolute positioning.
  5. Top toolbar (childPageSubnav) stays pinned right of the subnav header -- never slides underneath when collapsed.
  6. Fixed pre-existing build errors: `ViewMode` type mismatch (`signal<string>` -> `signal<'grid' | 'list'>`), severity type overlap in `widget-agents.ts`.
- **Decisions**:
  - `CollapsibleSubnavComponent` uses two nested divs: outer for layout width (0 or 227), inner absolute-positioned at 227px for visual rendering.
  - Canvas mode: `tileSubnavWidth` is 0 when collapsed, `tileToolbarLeft`/`tileToolbarWidth` always use expanded width so toolbar doesn't shift.
  - Non-canvas mode: toolbar wrapped in div with conditional `margin-left: 227px` when collapsed.
  - Collapsed subnav locked rect: 227px wide, 48px tall (header only). Expanded: 227px wide, 600px tall.
  - `onItemSelect` auto-collapses the subnav after selecting an item.

### Session 8 (Mar 29)
- **Topic**: Optimization, refactoring, PROGRESS.md update, skills update
- **What happened**:
  1. Analyzed codebase for optimization opportunities. Identified weather icon/color helpers duplicated across 4 files.
  2. Consolidated shared utility functions into `dashboard-data.ts`: `weatherIcon()`, `weatherIconColor()`, `workImpactBadge()`. Removed duplicate static maps from `project-dashboard.component.ts`, `home-page.component.ts`, `records-subpages.component.ts`, `record-detail-views.component.ts`.
  3. Updated `PROGRESS.md` with Phase 8 (18 items) covering agentic enhancements, AI navigation, financial deep-linking, smart home widgets, weather integration, and code quality. Test coverage updated to 184 static tests across 8 files.
  4. Updated `dashboard-layout-lessons` skill with new section 12: shared utility functions pattern.
  5. Created new `navigation-routing` skill covering dynamic back buttons, shell title overrides, route-based deep linking, agentic page wiring, and deep-linking from widgets.
  6. Updated `agentic-widgets` skill with sections 9 (route-based actions) and 10 (domain-specific agents).
- **Decisions**:
  - All mapping functions (icon, color, badge) that appear in more than one component go in `dashboard-data.ts`.
  - Components keep thin delegate methods that call the shared function (preserves template compatibility).

## Active Context

- On `dev` branch, synced with `main`.
- Refactoring and skill updates complete. Build, type-check, and 184 static tests all pass.
- Pre-existing SVG color lint violations remain (AI icon gradients) -- not related to changes.

## Pending Items

- (none)

## Scratch Notes

- Total commits: 155+
- Phase 8 added to PROGRESS.md: 18 new items, bringing total to 85/93 (91%)
- New skill: `navigation-routing` at `.cursor/skills/navigation-routing/SKILL.md`
