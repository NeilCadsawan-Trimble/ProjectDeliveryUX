---
description: "MANDATORY FIRST ACTION: Check if 'Current window' date below is older than today. If stale, you MUST consolidate BEFORE doing anything else. This is not optional. Read the STALENESS CHECK section immediately."
alwaysApply: true
---

# STALENESS CHECK — DO THIS FIRST

**Current window**: Apr 13, 2026

**TODAY'S DATE**: Compare the "Current window" above against today's date (from the system prompt).

**IF THE DATE ABOVE IS OLDER THAN TODAY, STOP. You MUST consolidate before responding to the user's query.**

Consolidation steps (do all of these NOW, before anything else):
1. Read `longterm-memory.mdc`.
2. Move any lasting decisions, preferences, or patterns from the sessions below into the appropriate section of `longterm-memory.mdc`.
3. Add each session to the Chat Session History table in `longterm-memory.mdc`.
4. Clear everything below the `---` separator in THIS file.
5. Update "Current window" above to today's date.
6. Start a fresh "Today's Sessions" section.
7. THEN proceed with the user's request.

If the date matches today, skip this section and proceed normally.

# Short-Term Memory

> During each session, append interactions below under the current date.
> Only keep entries that are relevant to the *current* working day below.

---

## Today's Sessions

*(Apr 13, 2026)*

### Session 22: Fix canvas mode for Frank/Bert
- HOME_DEFAULT_LAYOUT seed was overwritten with Pamela's 8-widget estimator layout during commit 6c352a3
- Restored correct 18-widget Frank/Bert layout from HEAD~2
- FINANCIALS_DEFAULT_LAYOUT also had dimension changes restored (finNavKpi height, colSpan swaps)
- Build passing after fix

### Session 23: Fully independent per-persona layout seeds
- **Eliminated ALL shared "default" seed files.** 5 personas x 4 pages = 20 independent seed files.
- Deleted: `home-default.layout.ts`, `financials-default.layout.ts`, `projects-default.layout.ts`, `project-detail.layout.ts`
- Created per-persona seeds: `{page}-{persona}.layout.ts` for all combinations
- Updated routing in all 4 page components (`home-page`, `financials-page`, `projects-page`, `project-dashboard`) to use per-persona switch statements
- Updated `buildProjectsLayoutConfig()` to accept seed as parameter (no hardcoded import)
- Updated `project-dashboard` `getEngineConfig()` to use `this.getLayoutSeedForCurrentPersona()` (not hardcoded)
- Updated export tool map in `dashboard-shell` to map every `{page}:{persona}` combination
- Fixed `canvas-grid-alignment.spec.ts` to scan seed files instead of component files (was silently empty)
- Rewrote `layout-seeds.spec.ts`: 288 tests covering file existence, widget lists, contamination, geometry, routing, and export map
- Updated SKILL.md section 37, longterm-memory Known Fragile Areas, Project File Map
- Build passes, 700+ static tests pass

### Session 24: Update skills and PROGRESS.md
- Updated PROGRESS.md: added Phase 24 (Pamela Chen), Phase 25 (Independent Layout Seeds), renumbered Phase 26 (Remaining Work)
- Updated regression test coverage table (1280+ tests, updated per-file counts)
- Updated chat sessions table with Apr 12 and Apr 13 sessions
- Updated longterm-memory: session 22 added, static test count updated to 1280+
- Updated layout seeds directory description in Key Files

## Active Context

- On `dev` branch.
- Changes not yet pushed to GitHub.

## Pending Items

- Changes not yet pushed to GitHub.
