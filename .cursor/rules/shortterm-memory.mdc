---
description: "MANDATORY FIRST ACTION: Check if 'Current window' date below is older than today. If stale, you MUST consolidate BEFORE doing anything else. This is not optional. Read the STALENESS CHECK section immediately."
alwaysApply: true
---

# STALENESS CHECK — DO THIS FIRST

**Current window**: Apr 17, 2026

**TODAY'S DATE**: Compare the "Current window" above against today's date (from the system prompt).

**IF THE DATE ABOVE IS OLDER THAN TODAY, STOP. You MUST consolidate before responding to the user's query.**

Consolidation steps (do all of these NOW, before anything else):
1. Read `longterm-memory.mdc`.
2. Move any lasting decisions, preferences, or patterns from the sessions below into the appropriate section of `longterm-memory.mdc`.
3. Add each session to the Chat Session History table in `longterm-memory.mdc`.
4. Clear everything below the `---` separator in THIS file.
5. Update "Current window" above to today's date.
6. Start a fresh "Today's Sessions" section.
7. THEN proceed with the user's request.

If the date matches today, skip this section and proceed normally.

# Short-Term Memory

> During each session, append interactions below under the current date.
> Only keep entries that are relevant to the *current* working day below.

---

## Today's Sessions

*(Apr 17, 2026)*

### Session 26: Fix canvas project tile navigation
- Project tile links in canvas mode on projects dashboard not working
- Root cause: `pointer-events: none !important` on `.canvas-content > *` in `src/styles.css` line 1920 blocked ALL interactions (drag, resize, click) in canvas mode
- Fix: removed that `pointer-events` rule; spacebar-pan pointer-events already handled by `.canvas-pan-ready` and `.canvas-panning` rules

### Session 27: Fix canvas KPI cards cut off
- KPI cards in projHeader widget clipped at bottom in canvas mode
- Root cause: `canvasDefaultHeights.projHeader` was 144px but content needs ~154px (title + margin + KPI cards with subtext)
- Fix: increased projHeader canvas height to 176px in all 5 persona seed files, shifted all other widget canvasDefaultTops +32px

### Session 28: Modus Typography full migration (attempt 2)
- User pointed out `<div class="text-2xs text-foreground-40 uppercase tracking-wide">SCHEDULE</div>` not using modus-typography
- Prior session (7ebe8d8c) claimed migration was complete but left ~310 lines across 32 files unconverted
- Root cause of prior failure: subagents were inconsistent, `text-2xs` patterns skipped (no direct size mapping), no post-migration audit
- Completed full migration: all raw Tailwind text sizing on div/span elements converted to `<modus-typography>` across all pages, shared, and shell components
- `text-2xs` mapped to `size="xs" className="text-2xs"` (custom CSS override preserved in className)
- `font-medium` mapped to `weight="semibold"` (closest design system weight)
- Added static regression test: `tests/static/typography-enforcement.spec.ts` — scans all component templates for raw text-size/font-weight classes on div/span, excluding icons/inputs/code/className
- All gates pass: `ng build` (0 errors), `lint:all` (7/7), `type-check` (0 errors), static tests (1286 passed across 15 files)
- Exceptions left as-is: `innerHTML` rendered HTML in AI assistant (can't use Angular components in dynamic HTML)

## Active Context

- On `dev` branch.
- Changes not yet pushed to GitHub.

## Pending Items

- Changes not yet pushed to GitHub.
