---
description: "MANDATORY FIRST ACTION: Check if 'Current window' date below is older than today. If stale, you MUST consolidate BEFORE doing anything else. This is not optional. Read the STALENESS CHECK section immediately."
alwaysApply: true
---

# STALENESS CHECK — DO THIS FIRST

**Current window**: August 20, 2026

**TODAY'S DATE**: Compare the "Current window" above against today's date (from the system prompt).

**IF THE DATE ABOVE IS OLDER THAN TODAY, STOP. You MUST consolidate before responding to the user's query.**

Consolidation steps (do all of these NOW, before anything else):
1. Read `longterm-memory.mdc`.
2. Move any lasting decisions, preferences, or patterns from the sessions below into the appropriate section of `longterm-memory.mdc`.
3. Add each session to the Chat Session History table in `longterm-memory.mdc`.
4. Clear everything below the `---` separator in THIS file.
5. Update "Current window" above to today's date.
6. Start a fresh "Today's Sessions" section.
7. THEN proceed with the user's request.

If the date matches today, skip this section and proceed normally.

# Short-Term Memory

> During each session, append interactions below under the current date.
> Only keep entries that are relevant to the *current* working day below.

---

## Today's Sessions

*(August 20, 2026)*

- Exploring static seed data inventory for all date-bearing types (calendar, RFIs, COs, invoices, AP, drawings, etc.) ahead of a bulk date refresh.
- Implementing dated-seed refresh: `SEED_AS_OF` = Aug 20, 2026; persona calendars; project dues; RFIs/COs/schedules/financials through Dec 2026.
- **Completed seed refresh (Aug–Dec 2026):** `SEED_AS_OF` helpers; distinct calendars for Frank/Bert/Kelly/Dominique/Pamela; Transit Hub due Dec 18 (status Overdue, recovery copy); Metro Bridge due Nov 20; Gantt/RFI/CO/financials/payroll through Dec. `npm run lint:all` + `npx ng build` green. Pre-existing 1.06 MB bundle budget warning unchanged. Static tests and layout seeds not edited.
- **Completed email + chat widgets:** Role-specific inboxes and shared team chat on every home and project dashboard. `npm run lint:all`, `npx ng build`, `npm run test:static` (1671), and layout-seed-clearance (41) green. Pre-existing 1.06 MB bundle budget warning unchanged.
- Project landing Team Chat now shows only that project's channel (Kelly still Internal Work). Home picker unchanged.

## Active Context

- Jun 29 npm-task-detection fix (`scripts/prepare.mjs` + `package.json` prepare/start script changes) may still be uncommitted locally — verify before push.
- Dependency baseline from Jun 29: pdfjs-dist 5.7.284, @angular/cli 21.2.12, @angular/common 21.2.14, jsdom 29.1.1, @deepgram/sdk 5.3.0.
- Initial-bundle budget warning at 1.06 MB vs 800 kB cap (unchanged).

## Pending Items

- Confirm Cursor Tasks output channel is silent after the prepare.mjs fix (user reload).
- Voice input (Deepgram) still awaits manual browser mic verification.
- Consider lazy-loading `@deepgram/sdk` if bundle budget becomes a hard failure.
- Manual smoke for docked Trimble Assistant panel (PR #139).
- Manual smoke for email/chat widgets: each persona home (expand, send, Kelly one channel); one project dashboard (project-filtered mail, default project channel); Dominique via `/dominique/...`.
